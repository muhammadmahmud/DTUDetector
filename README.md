# DTUDetector
This application is developed to detect any projects in a repository that is using Azure DTU SKU. This will help engineers reducing the time to inspect the repository manually.

Steps:
1. Update the 'root' variable to local repository path.
2. Build & run the program.
